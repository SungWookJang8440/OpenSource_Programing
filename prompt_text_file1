[요구사항]
내 웹 서비스 코드에서 두 가지 문제(스멜)를 발견했어.
1. `app.py`에서 `render_template`에 9개의 변수(name, tagline 등)를 일일이 나열해서 전달하는 Data Clumps 문제와 코드가 중복되는 문제.
2. `validator.py`에서 if문이 계속 추가되는 OCP 위반 문제.
이 두 가지 문제를 해결하기 위한 리팩토링 전략을 각각 제안해 줘.